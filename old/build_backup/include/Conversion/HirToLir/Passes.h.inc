/* Autogenerated by mlir-tblgen; don't manually edit */
#ifdef GEN_PASS_CLASSES

//===----------------------------------------------------------------------===//
// HirToLirPass
//===----------------------------------------------------------------------===//

template <typename DerivedT>
class HirToLirPassBase : public ::mlir::OperationPass<ModuleOp> {
public:
  using Base = HirToLirPassBase;

  HirToLirPassBase() : ::mlir::OperationPass<ModuleOp>(::mlir::TypeID::get<DerivedT>()) {}
  HirToLirPassBase(const HirToLirPassBase &other) : ::mlir::OperationPass<ModuleOp>(other) {}

  /// Returns the command-line argument attached to this pass.
  static constexpr ::llvm::StringLiteral getArgumentName() {
    return ::llvm::StringLiteral("convert-hir-to-lir");
  }
  ::llvm::StringRef getArgument() const override { return "convert-hir-to-lir"; }

  ::llvm::StringRef getDescription() const override { return "Convert hir dialect to lir dialect"; }

  /// Returns the derived pass name.
  static constexpr ::llvm::StringLiteral getPassName() {
    return ::llvm::StringLiteral("HirToLirPass");
  }
  ::llvm::StringRef getName() const override { return "HirToLirPass"; }

  /// Support isa/dyn_cast functionality for the derived pass class.
  static bool classof(const ::mlir::Pass *pass) {
    return pass->getTypeID() == ::mlir::TypeID::get<DerivedT>();
  }

  /// A clone method to create a copy of this pass.
  std::unique_ptr<::mlir::Pass> clonePass() const override {
    return std::make_unique<DerivedT>(*static_cast<const DerivedT *>(this));
  }

  /// Return the dialect that must be loaded in the context before this pass.
  void getDependentDialects(::mlir::DialectRegistry &registry) const override {
    
  registry.insert<hir::HirDialect>();

  registry.insert<clir::CLirDialect>();

  registry.insert<qlir::QLirDialect>();

  registry.insert<lir::LirDialect>();

  registry.insert<StandardOpsDialect>();

  registry.insert<AffineDialect>();

  registry.insert<scf::SCFDialect>();

  }

protected:
};
#undef GEN_PASS_CLASSES
#endif // GEN_PASS_CLASSES
#ifdef GEN_PASS_REGISTRATION

//===----------------------------------------------------------------------===//
// HirToLirPass Registration
//===----------------------------------------------------------------------===//

inline void registerHirToLirPassPass() {
  ::mlir::registerPass([]() -> std::unique_ptr<::mlir::Pass> {
    return mlir::createHirToLirPass();
  });
}

//===----------------------------------------------------------------------===//
// HirToLirConversion Registration
//===----------------------------------------------------------------------===//

inline void registerHirToLirConversionPasses() {
  registerHirToLirPassPass();
}
#undef GEN_PASS_REGISTRATION
#endif // GEN_PASS_REGISTRATION
