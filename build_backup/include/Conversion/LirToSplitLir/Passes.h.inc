/* Autogenerated by mlir-tblgen; don't manually edit */
#ifdef GEN_PASS_CLASSES

//===----------------------------------------------------------------------===//
// LirToSplitLirPass
//===----------------------------------------------------------------------===//

template <typename DerivedT>
class LirToSplitLirPassBase : public ::mlir::OperationPass<ModuleOp> {
public:
  using Base = LirToSplitLirPassBase;

  LirToSplitLirPassBase() : ::mlir::OperationPass<ModuleOp>(::mlir::TypeID::get<DerivedT>()) {}
  LirToSplitLirPassBase(const LirToSplitLirPassBase &other) : ::mlir::OperationPass<ModuleOp>(other) {}

  /// Returns the command-line argument attached to this pass.
  static constexpr ::llvm::StringLiteral getArgumentName() {
    return ::llvm::StringLiteral("convert-lir-to-split-lir");
  }
  ::llvm::StringRef getArgument() const override { return "convert-lir-to-split-lir"; }

  ::llvm::StringRef getDescription() const override { return "Convert lir dialect to c-lir and q-lir dialects"; }

  /// Returns the derived pass name.
  static constexpr ::llvm::StringLiteral getPassName() {
    return ::llvm::StringLiteral("LirToSplitLirPass");
  }
  ::llvm::StringRef getName() const override { return "LirToSplitLirPass"; }

  /// Support isa/dyn_cast functionality for the derived pass class.
  static bool classof(const ::mlir::Pass *pass) {
    return pass->getTypeID() == ::mlir::TypeID::get<DerivedT>();
  }

  /// A clone method to create a copy of this pass.
  std::unique_ptr<::mlir::Pass> clonePass() const override {
    return std::make_unique<DerivedT>(*static_cast<const DerivedT *>(this));
  }

  /// Return the dialect that must be loaded in the context before this pass.
  void getDependentDialects(::mlir::DialectRegistry &registry) const override {
    
  registry.insert<hir::HirDialect>();

  registry.insert<clir::CLirDialect>();

  registry.insert<qlir::QLirDialect>();

  registry.insert<lir::LirDialect>();

  registry.insert<StandardOpsDialect>();

  registry.insert<AffineDialect>();

  registry.insert<scf::SCFDialect>();

  }

protected:
};

//===----------------------------------------------------------------------===//
// SplitFuncPass
//===----------------------------------------------------------------------===//

template <typename DerivedT>
class SplitFuncPassBase : public ::mlir::OperationPass<FuncOp> {
public:
  using Base = SplitFuncPassBase;

  SplitFuncPassBase() : ::mlir::OperationPass<FuncOp>(::mlir::TypeID::get<DerivedT>()) {}
  SplitFuncPassBase(const SplitFuncPassBase &other) : ::mlir::OperationPass<FuncOp>(other) {}

  /// Returns the command-line argument attached to this pass.
  static constexpr ::llvm::StringLiteral getArgumentName() {
    return ::llvm::StringLiteral("split-lir-funcs");
  }
  ::llvm::StringRef getArgument() const override { return "split-lir-funcs"; }

  ::llvm::StringRef getDescription() const override { return "Split lir operands in different functions"; }

  /// Returns the derived pass name.
  static constexpr ::llvm::StringLiteral getPassName() {
    return ::llvm::StringLiteral("SplitFuncPass");
  }
  ::llvm::StringRef getName() const override { return "SplitFuncPass"; }

  /// Support isa/dyn_cast functionality for the derived pass class.
  static bool classof(const ::mlir::Pass *pass) {
    return pass->getTypeID() == ::mlir::TypeID::get<DerivedT>();
  }

  /// A clone method to create a copy of this pass.
  std::unique_ptr<::mlir::Pass> clonePass() const override {
    return std::make_unique<DerivedT>(*static_cast<const DerivedT *>(this));
  }

  /// Return the dialect that must be loaded in the context before this pass.
  void getDependentDialects(::mlir::DialectRegistry &registry) const override {
    
  registry.insert<hir::HirDialect>();

  registry.insert<clir::CLirDialect>();

  registry.insert<qlir::QLirDialect>();

  registry.insert<lir::LirDialect>();

  registry.insert<StandardOpsDialect>();

  }

protected:
};
#undef GEN_PASS_CLASSES
#endif // GEN_PASS_CLASSES
#ifdef GEN_PASS_REGISTRATION

//===----------------------------------------------------------------------===//
// LirToSplitLirPass Registration
//===----------------------------------------------------------------------===//

inline void registerLirToSplitLirPassPass() {
  ::mlir::registerPass([]() -> std::unique_ptr<::mlir::Pass> {
    return mlir::createLirToSplitLirPass();
  });
}

//===----------------------------------------------------------------------===//
// SplitFuncPass Registration
//===----------------------------------------------------------------------===//

inline void registerSplitFuncPassPass() {
  ::mlir::registerPass([]() -> std::unique_ptr<::mlir::Pass> {
    return mlir::createSplitFuncPass();
  });
}

//===----------------------------------------------------------------------===//
// LirToSplitLirConversion Registration
//===----------------------------------------------------------------------===//

inline void registerLirToSplitLirConversionPasses() {
  registerLirToSplitLirPassPass();
  registerSplitFuncPassPass();
}
#undef GEN_PASS_REGISTRATION
#endif // GEN_PASS_REGISTRATION
